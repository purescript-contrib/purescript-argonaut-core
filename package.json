{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "devDependencies": {
    "bower": "^1.7.9",
    "pulp": "^9.0.0",
    "purescript": "^0.9.1-rc.1",
    "rimraf": "^2.5.2"
  }
}
