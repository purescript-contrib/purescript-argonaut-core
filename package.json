{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^5.0.2",
    "purescript": "^0.8.0",
    "rimraf": "^2.4.4"
  }
}
